@page "/"
@using Microsoft.Extensions.DependencyInjection
@using Microsoft.EntityFrameworkCore


<PageTitle>DarkHardware Hub</PageTitle>

<div class="search-container">
    <form @onsubmit="HandleSearch" class="search-form">
        <input type="text" placeholder="Search products..." @bind="searchQuery" class="search-input" />
        <button type="submit" class="search-button">Search</button>
    </form>
</div>


<div class="header">
    <p>Newest Items For Sale</p>
</div>

<div class="product-list">
    <!-- Produktkort 1 -->
    <div class="product-card" @onclick="() => VisProduktDetaljer(1)">
        <img src="FlipperZero.png" alt="Flipper Zero">
        <div class="product-details">
            <h3>Flipper Zero</h3>
            <p>Unlock the world of possibilities with the Flipper Zero, your trusty sidekick in the realm of hacking and security exploration.</p>
            <p class="price">Price: $100</p>
        </div>
    </div>

    <!-- Produktkort 2 -->
    <div class="product-card" @onclick="() => VisProduktDetaljer(2)">
        <img src="UsbKiller.png" alt="Usb Killer">
        <div class="product-details">
            <h3>Usb Killer</h3>
            <p>Powerful tool for security testing and awareness. Used, effective, ready."</p>
            <p class="price">Price: $60</p>
        </div>
    </div>

    <!-- Produktkort 3 -->
    <div class="product-card" @onclick="() => VisProduktDetaljer(3)">
        <img src="geforce4090.png" alt=" GeForce RTX 4090 - Graphics Card">
        <div class="product-details">
            <h3>GeForce RTX 4090</h3>
            <p>The graphics card is not functioning; it's dead. Perhaps someone can fix it and get it working again.</p>
            <p class="price">Price: $600</p>
        </div>
    </div>
</div>

@code {
    private string searchQuery = "";

    [Inject]
    private AppDbContext context { get; set; }

    // Konstruktør til initialisering af context
    public Index()
    {
        context = new AppDbContext(new DbContextOptions<AppDbContext>());
    }

    void HandleSearch()
    {
        // Implementer søgelogik her, for eksempel filtrer produktlisten baseret på søgeforespørgslen

        // Brug den filtrerede liste som din produktliste
    }

    // Metode til at håndtere klik på "Se detaljer" knappen (IKKE TILFØJET ENDNU)
    void VisProduktDetaljer(int produktId)
    {
        // Implementer detaljer handling her
    }
}


<style>
    body {
        background: linear-gradient(to bottom, #052767, #3a0647);
    }

    .header {
        text-align: center;
        margin-bottom: 20px;
    }

        .header h1,
        .header p {
            color: #0373fc;
            font-family: 'Roboto', sans-serif;
            font-size: 2em;
        }

    .product-list {
        display: flex;
        flex-wrap: wrap;
        justify-content: space-around;
    }

    .product-card {
        display: flex;
        flex-direction: column;
        border: 1px;
        background-color: #222;
        padding: 15px;
        margin: 10px;
        width: 220px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        transition: transform 0.3s;
        text-align: center;
        border-radius: 8px;
    }

        .product-card:hover {
            transform: scale(1.05);
        }

    .product-details {
        flex-grow: 1;
        display: flex;
        flex-direction: column;
    }

    .price {
        margin-top: auto;
        background: linear-gradient(to right, #052767, #3a0647);
        color: #fff;
        padding: 10px;
        border-radius: 4px;
    }

    h3,
    p {
        color: #fff;
    }

    .header {
        text-align: center;
        margin-bottom: 20px;
        color: #fff;
    }

        .header h1, .header p {
            font-size: 2em;
        }


    .search-form {
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .search-input {
        padding: 10px;
        width: 300px;
        border: 1px solid #0373fc;
        border-radius: 4px;
        margin-right: 10px;
    }

    .search-button {
        padding: 10px;
        background-color: #0373fc;
        color: #fff;
        border: none;
        cursor: pointer;
        border-radius: 4px;
    }

        .search-button:hover {
            background-color: #0056b3;
        }

</style>
